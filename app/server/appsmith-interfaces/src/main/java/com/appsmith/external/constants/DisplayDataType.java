package com.appsmith.external.constants;

/*
 * - Please order the types according to their priority. The types will be sent back to the client with the same
 * priority order.
 */
public enum DisplayAppsmithType {
    TABLE,
    JSON,
    RAW,
}
