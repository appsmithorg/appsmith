FOR user IN users
FILTER user.role == "Admin"
SORT user.id ASC
LIMIT 10
RETURN user

//Use widget data in a query using {{ widgetName.property }}.
//Read more at https://www.arangodb.com/docs/stable/aql/