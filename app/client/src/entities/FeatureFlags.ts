type FeatureFlags = {
  APP_TEMPLATE?: boolean;
  JS_EDITOR?: boolean;
  MULTIPLAYER?: boolean;
  SNIPPET?: boolean;
  GIT?: boolean;
  GIT_IMPORT?: boolean;
  RBAC?: boolean;
};

export default FeatureFlags;
