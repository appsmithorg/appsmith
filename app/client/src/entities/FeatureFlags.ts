type FeatureFlags = {
  APP_TEMPLATE?: boolean;
  JS_EDITOR?: boolean;
  MULTIPLAYER?: boolean;
  SNIPPET?: boolean;
  GIT?: boolean;
  GIT_IMPORT?: boolean;
  TEMPLATES_PHASE_2?: boolean;
  RBAC?: boolean;
  AUDIT_LOGS?: boolean;
  CONTEXT_SWITCHING?: boolean;
  USAGE?: boolean;
};

export default FeatureFlags;
