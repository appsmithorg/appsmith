type FeatureFlags = {
  TEST_FLAG?: boolean;
  DATASOURCE_ENVIRONMENTS?: boolean;
  AUTO_LAYOUT?: boolean;
  ask_ai?: boolean;
  APP_NAVIGATION_LOGO_UPLOAD?: boolean;
  ask_ai_sql?: boolean;
  ask_ai_js?: boolean;
  APP_EMBED_VIEW_HIDE_SHARE_SETTINGS_VISIBILITY?: boolean;
};

export default FeatureFlags;
