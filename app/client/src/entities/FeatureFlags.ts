type FeatureFlags = {
  TEST_FLAG?: boolean;
  DATASOURCE_ENVIRONMENTS?: boolean;
  AUTO_LAYOUT?: boolean;
  ask_ai?: boolean;
  APP_NAVIGATION_LOGO_UPLOAD?: boolean;
};

export default FeatureFlags;
