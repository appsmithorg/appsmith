type FeatureFlags = {
  APP_TEMPLATE?: boolean;
  JS_EDITOR?: boolean;
  MULTIPLAYER?: boolean;
  SNIPPET?: boolean;
  GIT?: boolean;
  GIT_IMPORT?: boolean;
  TEMPLATES_PHASE_2?: boolean;
  RBAC?: boolean;
  AUDIT_LOGS?: boolean;
};

export default FeatureFlags;
