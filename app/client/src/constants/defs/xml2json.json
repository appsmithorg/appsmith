{
  "!name": "xml-js",
  "xml2json": {
    "!doc": "converts xml string to json string",
    "!type": "fn(xml: string, options: object) -> string"
  },
  "json2xml": {
    "!doc": "converts json string to xml string",
    "!type": "fn(json: string, options: object) -> string"
  }
}
