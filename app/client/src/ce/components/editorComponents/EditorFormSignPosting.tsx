import type { TEditorModes } from "components/editorComponents/CodeEditor/EditorConfig";

export type EditorSignPostingProps = {
  isAIEnabled: boolean;
  mode: TEditorModes;
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function EditorFormSignPosting(props: EditorSignPostingProps) {
  return null;
}
