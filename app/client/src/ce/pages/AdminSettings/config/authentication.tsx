import authConfig from "pages/Settings/config/authentication";
import React from "react";
export default authConfig;

export const authMain = () => {
  return <h1>Test Auth Community Component</h1>;
};
