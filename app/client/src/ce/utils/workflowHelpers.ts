import type { Def } from "tern";

export const useWorkflowOptions = () => {
  return [];
};

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const workflowAutocompleteDefinitions = (generatedTypeDef: Def) => ({});
