import React from "react";
import {
  IconButton as CIconButton,
  IconButtonProps as CIconButtonProps,
} from "@chakra-ui/react";

function IconButton(props: CIconButtonProps) {
  return <CIconButton {...props} />;
}

export default IconButton;
