import { WIDGET_TAGS } from "constants/WidgetConstants";

export const metaConfig = {
  name: "Button",
  needsMeta: false,
  isCanvas: false,
  tags: [WIDGET_TAGS.SUGGESTED_WIDGETS, WIDGET_TAGS.BUTTONS],
  searchTags: ["click", "submit", "button", "action", "run"],
};
