import { WIDGET_TAGS } from "constants/WidgetConstants";

export const metaConfig = {
  name: "Icon Button",
  tags: [WIDGET_TAGS.BUTTONS],
  searchTags: ["click", "submit", "button", "action", "run", "icon", "pic"],
};
