/* eslint-disable @typescript-eslint/no-unused-vars*/
export default {
  getFormattedText: (props, moment, _) => {
    if (props.isFocused) {
      return props.text;
    }
    return props.text;
  },
  //
};
