import type { AnvilConfig } from "WidgetProvider/types";

export const anvilConfig: AnvilConfig = {
  isLargeWidget: true,
  widgetSize: {
    minWidth: { base: "100%" },
    minHeight: { base: "auto" },
  },
};
