import type { AnvilConfig } from "WidgetProvider/constants";

export const anvilConfig: AnvilConfig = {
  isLargeWidget: true,
  widgetSize: {
    minWidth: { base: `120px` },
    minHeight: { base: "50px" },
  },
};
