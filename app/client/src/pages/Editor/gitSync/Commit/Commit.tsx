import TextInput from "components/ads/TextInput";
import React from "react";
import { Title } from "../StyledComponents";

const commitTitle = () => "Commit";

/**
 * - Check if the app has any uncommitted changes before showing an option to commit changes
 * - Show the current branch and an option to create a new branch
 * - Fetch or put autogenerated commit message
 * - Show last commit time and message
 * - Button to push changes: show number of changes still not pushed
 * - Sync or pull latest from remote
 * - Merge flow -> merge before committing
 * - Checkout new branch and commit
 */
function Commit() {
  return (
    <>
      <Title>{commitTitle()}</Title>
      <TextInput
        onChange={(value: string) => {
          console.log(value);
        }}
        placeholder={"Autogenerated commit message"}
      />
    </>
  );
}

export default Commit;
