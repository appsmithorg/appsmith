import React from "react";
import styled from "styled-components";

const Body = styled.div`
  background: #f1f5f9;
  height: calc(100vh - 40px);
`;

const IDE = function () {
  return <Body id="IDE-body" />;
};

export default IDE;
