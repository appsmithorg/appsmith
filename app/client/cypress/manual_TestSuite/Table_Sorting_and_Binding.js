const dsl = require("../../../fixtures/tableWidgetDsl.json");


describe("Sorting and binding ", function() {
    it("Test table Sorting and binding", function() 
    {
        //Add a table
        //Click on the column and observe the sorting
        // Click on the property pane
        // Navigate to Default selected Row 
        // Add an index
        // Add an input widget
        // Click to open property pane
        // Navigate to Default text
        // Add a binding to reflect any column 
        // and observe that as we sort the input widget must get updated 
    }
    )
}
)
