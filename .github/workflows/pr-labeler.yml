name: Label PRs based on title

on:
  pull_request:
    branches: [release]
    types: [opened, reopened, edited]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: appsmithorg/labeler@master
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
      
      - name: Get changed files using defaults
        id: changed-files
        uses: trilom/file-changes-action@v1.2.3
        continue-on-error: true
      
      - name: Run step when a file changes
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
        if: contains(steps.changed-files.outputs.files_modified, 'cypress') == false
<<<<<<< HEAD
        run: |
          echo "Your my-file.txt file has been modified."
=======
          echo "asda"
>>>>>>> 66cdb2a6e972c83a12e75362db3ea9c39b3c69a5

      


