# Specify commands for issues and pull requests
# ---------------------------------------------

# Actions taken when the `Datasources` label is added
Datasources:
  labels: ['advanced']
  lock: true
