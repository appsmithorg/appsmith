version: "3"

services:
  appsmith:
    image: index.docker.io/kienmanabie/appsmith-ce-custom:0.0.3
    container_name: appsmith
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./stacks:/appsmith-stacks
    restart: unless-stopped